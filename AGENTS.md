# Agent Instructions

- Update this `AGENTS.md` only when the user explicitly asks to remember something or clearly corrects a recurring preference or workflow.
- Keep only durable, reusable preferences here. Do not store one-off task notes or instructions already covered well by skills, MCPs, or tool documentation.
- Prefer consolidating, replacing, or deleting stale entries instead of appending near-duplicates.

## User-Specific Updates

- For client-facing message replies, provide the response inside a simple text code block for easy copy/paste.
- For guides and research-style work, including internal research artifacts, store the work in Notion rather than local Markdown files. Large research deliverables should be presented as polished Notion-ready pages.
- For live chat replies, do not add email-style sign-offs.
- For follow-up prompt tasks, treat the context as Upwork message threads, not email.
- For lead-handling workflow prompts, write agent instructions rather than only message-writing prompts.
- Keep follow-up prompts and lead-handling agent instructions as separate deliverables.
- When mentioning case studies, include actual links, preferably Upwork links, plus a short matching description.
- For proposal prompt work, create a brand new document instead of overwriting an existing one.
- The user's website on Webflow is `etavrian.com`.
- When the user asks to open or use a browser, interpret that as using Chrome MCP or Playwright MCP tools, not local headless browser scripts.
- For authenticated browser workflows such as GSC and Webflow, prefer MCP browser tools over local headless browser scripts.
