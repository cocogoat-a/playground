# Change Log

- Added an explicit pre-export account verification step to the Google Ads quarterly reporting workflow
- Added cleanup guidance to remove rows with `0` impressions and `0` cost
- Added Drive/Sheets replacement guardrails to avoid duplicate deliverables and unreliable upload-location shortcuts
- Fixed the workbook builder so zero-usefulness rows are removed automatically during CSV cleanup
